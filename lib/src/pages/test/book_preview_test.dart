import 'package:flutter/material.dart';

class BookPreviewTest extends StatelessWidget {
  const BookPreviewTest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(
        child: Stack(
          children: [],
        ),
      ),
    );
  }
}
